/* =========================================================
   fluGIT（GitHub Pages 静的版 管理画面）の設定

   このファイルは «書き換えて使う» ことを前提にしています．
   JSON ではなく JS にしてあるのは，file:// で直接開いて検証したときに
   fetch が CORS で失敗するのに対し，<script> なら必ず読めるためです．
   （LP本体が data/ScheduleData.js を <script> で読んでいるのと同じ理由）
========================================================= */
window.FLU_APP_CONFIG = {

	/* -----------------------------------------------------------------
	   本番LPの公開URL（末尾のスラッシュまで含めて書く）

	   ここを埋めると，管理画面を開いたときに
	       <lpBaseUrl>data/ScheduleData.js
	   を読みに行き，«いま本番サイトに出ている登録内容» を土台にします．

	   空のまま（''）にしておくと外部への通信は一切行わず，
	   ブラウザに保存された内容だけで起動します．
	   その場合，別のPC・別のブラウザで初めて開いたときは «空» から始まるため，
	   必ず「ファイルから読み込む」で現状を取り込んでから編集してください．

	   記入例:
	       lpBaseUrl: 'https://seijyo.yamabiko-group.or.jp/influenza/'
	   ----------------------------------------------------------------- */
	lpBaseUrl: '',

	/* -----------------------------------------------------------------
	   登録できる月の範囲（YYYY-MM）
	   LP側の表示期間と必ず一致させること．
	   ここから外れた日付は保存時に弾かれる（別年度への誤登録を防ぐため）．
	   ----------------------------------------------------------------- */
	displayRange: {
		from: '2026-08',
		to: '2027-03'
	},

	/* -----------------------------------------------------------------
	   ブラウザ内に残すバックアップの世代数
	   サーバ版（Config.json の backupGenerations）と揃えてある．
	   ----------------------------------------------------------------- */
	backupGenerations: 30
};
